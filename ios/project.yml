name: Nudget
options:
  bundleIdPrefix: app.nudget
  deploymentTarget:
    iOS: "17.0"
  createIntermediateGroups: true

settings:
  base:
    SWIFT_VERSION: "5.9"
    CODE_SIGN_STYLE: Automatic
    DEVELOPMENT_TEAM: VYC3499K46

packages:
  LinkKit:
    url: https://github.com/plaid/plaid-link-ios
    from: "6.4.7"

targets:
  Nudget:
    type: application
    platform: iOS
    sources:
      - Nudget
      - Shared
    dependencies:
      - package: LinkKit
      - target: NudgetWidget
        embed: true
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: app.nudget.ios
        MARKETING_VERSION: "0.1.0"
        CURRENT_PROJECT_VERSION: "1"
        TARGETED_DEVICE_FAMILY: "1" # iPhone
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
    entitlements:
      path: Nudget/Nudget.entitlements
      properties:
        com.apple.security.application-groups:
          - group.app.nudget.ios
        # APNs environment. TestFlight/App Store builds use PRODUCT tokens, so this
        # is "production". NOTE: local Xcode dev builds then push via sandbox tokens,
        # which won't deliver against the production host — test push via TestFlight.
        aps-environment: production
    info:
      path: Nudget/Info.plist
      properties:
        UILaunchScreen: {}
        UIApplicationSceneManifest:
          UIApplicationSupportsMultipleScenes: false
        UISupportedInterfaceOrientations:
          - UIInterfaceOrientationPortrait
        # Allow the dev server over http://localhost while developing.
        NSAppTransportSecurity:
          NSAllowsLocalNetworking: true
        # Standard HTTPS only → exempt from export compliance; set so TestFlight
        # doesn't ask the encryption question on every build.
        ITSAppUsesNonExemptEncryption: false

  NudgetWidget:
    type: app-extension
    platform: iOS
    sources:
      - NudgetWidget
      - Shared
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: app.nudget.ios.widget
        MARKETING_VERSION: "0.1.0"
        CURRENT_PROJECT_VERSION: "1"
        TARGETED_DEVICE_FAMILY: "1"
    entitlements:
      path: NudgetWidget/NudgetWidget.entitlements
      properties:
        com.apple.security.application-groups:
          - group.app.nudget.ios
    info:
      path: NudgetWidget/Info.plist
      properties:
        CFBundleDisplayName: Nudget
        NSExtension:
          NSExtensionPointIdentifier: com.apple.widgetkit-extension
